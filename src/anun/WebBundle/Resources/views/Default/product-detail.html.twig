{% extends '@anunWeb/Layout/base.html.twig' %}
{% block style %}
    <style>
        .item-container:hover .overlay {
            opacity: 0;
        }

        .detail-header {
            margin-top: 20px
        }

        @media only screen and (max-width: 767px) {
            .detail-header {
                margin-top: 100px
            }
        }
    </style>
{% endblock %}
{% block body %}
    <div id="about-us">
        <div class="about-us-2 about-us-body  text-center">
            <div class="container">
                <div class="row">

                    <div class="col-xs-12  wow bounceIn detail-header">
                        <div style="background-color: #fff;text-align: left">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="{{ path('home') }}">Нүүр хуудас</a></li>
                                <li class="breadcrumb-item"><a
                                            href="{% if paren1id == 1 %}{{ path('ger') }}{% else %}{{ path('albantasalgaa') }}{% endif %}">{{ paren1name }}</a>
                                </li>
                                <li class="breadcrumb-item"><a
                                            href="{{ path('product', {'id': paren2id}) }}">{{ paren2name }}</a></li>
                                <li class="breadcrumb-item active">{{ name }}</li>
                            </ol>
                        </div>
                    </div>

                    {% for a in alba %}
                        <div class="col-md-6 col-sm-12 col-xs-12 wow bounceIn">
                            <div class="container-fluid project-wrapper" id="MixItUp218D4D">
                                <div class="zerogrid">
                                    <div class="wrap-container clearfix">
                                        <div class="row wrap-content">
                                            {% for i in a.images %}
                                                <div class="col-2-4">
                                                    <div class="col-full mix work-item video"
                                                         style="display: inline-block;">
                                                        <div class="wrap-col">
                                                            <div class="item-container">
                                                                <a class="fancybox overlay text-center"
                                                                   data-fancybox-group="gallery"
                                                                   href="{{ image_path_item }}/{{ i.imgPath }}">
                                                                </a>
                                                                <img src="{{ image_path_item }}/{{ i.imgPath }}">
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6 col-sm-12 col-xs-12 wow bounceIn">
                            <div style="background-color: #fff;text-align: left">
                                {{ a.content|raw }}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block js %}
    <script src="{{ asset('resources/web/js/swiper.min.js') }}"></script>

    <script>
        var galleryTop = new Swiper('.gallery-top', {
            nextButton: '.swiper-button-next',
            prevButton: '.swiper-button-prev',
            spaceBetween: 10,
        });
        var galleryThumbs = new Swiper('.gallery-thumbs', {
            spaceBetween: 10,
            centeredSlides: true,
            slidesPerView: 'auto',
            touchRatio: 0.2,
            slideToClickedSlide: true
        });
        galleryTop.params.control = galleryThumbs;
        galleryThumbs.params.control = galleryTop;

    </script>
{% endblock %}